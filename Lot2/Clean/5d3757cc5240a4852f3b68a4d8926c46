Subject:Libreoffice en Ubuntu
Estimados:
Tengo Ubuntu:

# cat /etc/issue
Ubuntu 10.04.4 LTS \n \l

En el cual tenia instalado Libreoffice totalmente funcional.

Regularmente hago actualizacion de paquetes mediante apt-get update y =20
upgrade o mediante

# aptitude update -y && sudo aptitude dist-upgrade && aptitude clean

No se que paso, pero ya no tengo Libreoffice en mi ubuntu. Lo =20
desinstale totalmente y lo reinstale, sin embargo, me sale lo sgte. Me =20
pueden ayudar para poder dejar Libreoffice totalmente funcional? Una =20
vez desintalado con apt-get remove y purge hice lo sgte:

# add-apt-repository ppa:libreoffice/ppa

# apt-get update

#apt-get install libreoffice

#apt-get install libreoffice
Leyendo lista de paquetes... Hecho
Creando =E1rbol de dependencias
Leyendo la informaci=F3n de estado... Hecho
No se pudieron instalar algunos paquetes. Esto puede significar que
usted pidi=F3 una situaci=F3n imposible o, si est=E1 usando la distribuci=F3=
n
inestable, que algunos paquetes necesarios no han sido creados o han
sido movidos fuera de Incoming.
La siguiente informaci=F3n puede ayudar a resolver la situaci=F3n:

Los siguientes paquetes tienen dependencias incumplidas:
   libreoffice: Depende: libreoffice-core (=3D 1:3.5.4-0ubuntu1~lucid1) =20
pero no va a instalarse
                Depende: libreoffice-writer pero no va a instalarse
                Depende: libreoffice-calc pero no va a instalarse
                Depende: libreoffice-impress pero no va a instalarse
                Depende: libreoffice-draw pero no va a instalarse
                Depende: libreoffice-math pero no va a instalarse
                Depende: libreoffice-base pero no va a instalarse
                Depende: libreoffice-filter-mobiledev pero no va a instalars=
e
                Depende: libreoffice-java-common (>=3D 1:3.5.4~) pero no =20
va a instalarse
                Recomienda: libreoffice-gnome pero no va a instalarse o
                            libreoffice-kde pero no va a instalarse
E: Paquetes rotos

# apt-get install libreoffice-gnome
Leyendo lista de paquetes... Hecho
Creando =E1rbol de dependencias
Leyendo la informaci=F3n de estado... Hecho
No se pudieron instalar algunos paquetes. Esto puede significar que
usted pidi=F3 una situaci=F3n imposible o, si est=E1 usando la distribuci=F3=
n
inestable, que algunos paquetes necesarios no han sido creados o han
sido movidos fuera de Incoming.
La siguiente informaci=F3n puede ayudar a resolver la situaci=F3n:

Los siguientes paquetes tienen dependencias incumplidas:
   libreoffice-gnome: Depende: libreoffice-core (=3D =20
1:3.5.4-0ubuntu1~lucid1) pero no va a instalarse
                      Depende: libreoffice-gtk pero no va a instalarse
E: Paquetes rotos

#apt-get install libreoffice-l10n-es

Me pueden ayudar?

Gracias,
Mario Ramirez


-- 
To UNSUBSCRIBE, email to debian-user-spanish-REQUEST@lists.debian.org
with a subject of "unsubscribe". Trouble? Contact listmaster@lists.debian.org
Archive: http://lists.debian.org/20121021223903.9ro6b7sdgo4cs0kc@correo.dii.uchile.cl

